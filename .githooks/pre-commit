#!/bin/sh
set -e

if command -v pwsh >/dev/null 2>&1; then
  PSH=pwsh
else
  PSH=powershell
fi

"$PSH" -NoProfile -ExecutionPolicy Bypass -File "$(dirname "$0")/../scripts/verify.ps1"
compose-agentsmd --compose
git add AGENTS.md CLAUDE.md
